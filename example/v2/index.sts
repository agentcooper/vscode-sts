import { bar } from "./bar";
import { foo } from "./foo.sts";

export const data = {
  a: customSTSFunction(),
  b: 2,
  c: foo(),
  d: bar()
};

v1(); // should error
v2();

export const config = <Template name="John"></Template>;

