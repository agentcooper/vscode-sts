import { bar } from "./bar";
import { foo } from "./foo.sts";

export const data = {
  a: customSTSFunction(),
  b: 2,
  c: foo(),
  d: bar()
};

v1();
v2(); // should error

export const config = <Template name="John"></Template>;
